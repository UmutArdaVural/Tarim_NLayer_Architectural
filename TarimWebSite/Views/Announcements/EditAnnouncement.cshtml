@model EntityLayer.Concrete.Announcement
@{
    ViewData["Title"] = "EditAnnouncement";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}



<br />
<h1> </h1>
<div class="card">
    <div class="card-body">
        <h4 class="card-title">Duyuru Kayıt Düzenleme Formu  </h4>
        <p class="card-description">
            Duyuru Bilgiler
        </p>
        <form method="post" class="forms-sample">

            <div class="form-group">
                @Html.HiddenFor(x => x.AnnouncementId)
                <label for="exampleInputUsername1">Duyuru Başlığı </label>
                <input type="text" class="form-control" placeholder="Takım Arkadaşı İsmi " asp-for="Title">

                @if (ViewData.ModelState["Title"] != null && ViewData.ModelState["Title"].Errors.Any())
                {
                    <div class="alert alert-danger mt-2">
                        @foreach (var error in ViewData.ModelState["Title"].Errors)
                        {
                            <span>@error.ErrorMessage</span>

                            <br />
                        }
                    </div>
                }

            </div>
            <div class="form-group">
                <label for="exampleInputEmail1">Duyuru Bilgileri </label>
                <input type="text" class="form-control" placeholder="Görev" asp-for="Description">

                @if (ViewData.ModelState["Description"] != null && ViewData.ModelState["Description"].Errors.Any())
                {
                    <div class="alert alert-danger mt-2">
                        @foreach (var error in ViewData.ModelState["Description"].Errors)
                        {
                            <span>@error.ErrorMessage</span>

                            <br />
                        }
                    </div>
                }
            </div>
            <div class="form-group">
                <label for="exampleInputEmail1"> Oluşturulma Tarihi</label>
                <input type="text" class="form-control" placeholder="Resim Yolu" name="CreateDateTime" id="CreateDateTime" asp-for="CreateDateTime" readonly>
                @if (ViewData.ModelState["CreateDateTime"] != null && ViewData.ModelState["CreateDateTime"].Errors.Any())
                {
                    <div class="alert alert-danger mt-2">
                        @foreach (var error in ViewData.ModelState["CreateDateTime"].Errors)
                        {
                            <span>@error.ErrorMessage</span>

                            <br />
                        }
                    </div>
                }
            </div>
            <div class="form-group">
                <label for="exampleInputEmail1">Durum</label>
                <select style="color:blueviolet ; background-color:azure" class="form-control" id="Status" name="Status" asp-for="Status">
                    <option value="true">Aktif</option>
                    <option value="false">Pasif</option>
                </select>
                @if (ViewData.ModelState["Status"] != null && ViewData.ModelState["Status"].Errors.Any())
                {
                    <div class="alert alert-danger mt-2">
                        @foreach (var error in ViewData.ModelState["Status"].Errors)
                        {
                            <span>@error.ErrorMessage</span>
                            <br />
                        }
                    </div>
                }

            </div>
           
            <div class="form-check form-check-flat form-check-primary">
                <label class="form-check-label">
                    <input type="checkbox" class="form-check-input" id="onay" name="onay" value="true">
                    Onay
                </label>
            </div>
            @if (ViewData.ModelState["onay"] != null && ViewData.ModelState["onay"].Errors.Any())
            {
                <div class="alert alert-danger mt-2">
                    @ViewData.ModelState["onay"]?.Errors.FirstOrDefault()?.ErrorMessage
                </div>
            }

            <button type="submit" class="btn btn-primary me-2">Kayıt Güncelle </button>
            <a href="/Announcements/Index" class="btn btn-light">Geri Dön</a>
        </form>
    </div>

</div>
<br />






